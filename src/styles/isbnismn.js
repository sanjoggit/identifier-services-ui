import {makeStyles} from '@material-ui/core/styles';

const useStyles = makeStyles({
    IsbnIsmnContainer: {
        maxWidth: '1200px',
        margin: '0 auto',
        marginTop: '30px'
    }
});

export default useStyles;