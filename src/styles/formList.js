import {makeStyles} from '@material-ui/core/styles';

const useStyles = makeStyles({

    formListContainer: {
        width: '100%',
        background: '#b7bed629',
        height: '200px'
    }

});

export default useStyles;