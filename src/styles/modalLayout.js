import {makeStyles} from '@material-ui/core/styles';

const useStyles = makeStyles({

	main: {
		position: 'absolute',
		width: 400,
		backgroundColor: '#fff',
		outline: 'none'

	},
	button: {
		marginBottom: '20px'
	}

});

export default useStyles;
